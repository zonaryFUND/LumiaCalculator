import * as React from "react";
import TabUnit from "components/common/tab/tab-unit";

const buffDebuffs: React.FC = props => {
    return (
        <TabUnit title="バフ・デバフ">
            <p>作成中</p>
        </TabUnit>
    )
};

export default buffDebuffs;