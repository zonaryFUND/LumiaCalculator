import { SkillValueProps } from "../../damage-table";
import Constants from "../constants.json";

const table: SkillValueProps[] = [
    {label: "D移動速度増加量", skill: "D", value: Constants.pistol.movement_speed, type: "ms"}
]

export default table;