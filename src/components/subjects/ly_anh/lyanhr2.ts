import { TooltipInfo } from "../dictionary";

export const code = 1063510;

export const info: TooltipInfo = {
    skill: "R",
    values: ({ }) => ({}),
    expansion: () => ({
        enumeratedValues: []  
    })
}
