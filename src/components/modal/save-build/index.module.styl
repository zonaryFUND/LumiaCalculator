.save
    display: flex
    flex-direction: column
    align-items: stretch
    position: absolute
    background: var(--base)
    width: 600px
    height: 250px
    margin: auto
    inset: 0
    border-radius: 16px
    overflow: hidden
    box-shadow: 0 0 25px rgba(0,0,0,0.5)
    padding: 24px

    header
        display: flex
        justify-content: space-between
        align-items: center
        margin-bottom: 12px

        h1
            font-size: 24px

        p
            font-size: 12px

    input
        width: 100%
        height: 40px
        font-size: 20px
        padding: 0 8px

    button
        position: absolute
        color: white
        background: var(--c4)
        font-size: 24px
        font-weight: bold
        padding: 4px 16px
        right: 24px
        bottom: 24px

    button:disabled
        background: var(--lmg)
        cursor: default