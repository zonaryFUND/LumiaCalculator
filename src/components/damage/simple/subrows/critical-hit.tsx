import { Status } from "app-types/subject-dynamic/status/type";
import * as React from "react";

type Props = {
    status: Status
    ratio?: {
        base?: number
        attack?: number
    }
}

const criticalHit: React.FC<Props> = props => {
    
}

export defaut criticalHit;
